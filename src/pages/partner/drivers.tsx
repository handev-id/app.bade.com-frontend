import PartnerLayout from "./layout";

const Drivers = () => {
  return (
    <PartnerLayout header={{ title: "Drivers" }}>
      <div className="bg-neutral h-full">halo</div>
    </PartnerLayout>
  );
};

export default Drivers;
